# apps/web 환경변수 — 복사: cp .env.example .env.local (커밋 금지)

# 프록시 basePath — JupyterHub 에서만 필요, Vercel 배포 시 비워둠
# 예: NEXT_PUBLIC_BASE_PATH=/user/<학번>/proxy/3001
NEXT_PUBLIC_BASE_PATH=

# 백엔드 API 주소
# 로컬: http://localhost:8000  배포: Render URL
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
