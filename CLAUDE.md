# 개인별 맞춤형 정책 지원 분석 플랫폼

AI 화성 챌린지 주제2. 4인 팀, **8/19 23:59 웹 제출 마감**. 아래는 이 팀이 합의한 규칙이며 일반적인 관행보다 우선한다.

## 절대 규칙

**판정 경로에 LLM을 넣지 않는다.**
`evaluate(profile, policy, as_of)` 는 순수 함수다. 전역 상태 금지, LLM 호출 금지, 네트워크 호출 금지.
기준일은 `as_of` 인자로만 받는다 — `datetime.now()` 금지 (시연 영상 재현성·What-if 시간 이동의 전제).
- 조건이 애매해도 LLM으로 넘기지 말고 3분류 중 '서류 확인 필요' 로 분기한다
- LLM은 배치 파싱(정책 원문 → JSON)과 판정 후 요약 생성에만 쓴다
- 이유: "판정 경로에 LLM이 없어 환각에 의한 판정 오류가 구조적으로 불가능하다" 가 발표의 핵심 주장이다

**`exclusions` 는 판정에 쓰지 않는다.**
중복수급 제한은 화면 표시 전용이다. 판정 로직에 넣으면 검증 부담이 급증한다.

**서술형 조건은 `conditions` 에 넣지 않는다.**
기계 판정 값(숫자·보기 선택)만 `conditions` 에, 서술형 자격요건은 `manual_conditions` 로 (8/7 조건 이원화 결정).
반대로 파싱에서 서술형 조건을 통째로 빠뜨리는 것도 금지 — 조용한 과대 판정이 된다 (검수 체크리스트 ⑦이 잡는다).

**분류 규칙 0~4와 API 응답 형태의 정본은 `packages/schema/api-contract.md`(v1.1.4)다.**
특히 규칙 0(마감 종료 제외)과 규칙 2(충족 예정일 ≤ 마감일일 때만 upcoming)를 빠뜨리지 않는다.
응답의 verify 는 `{key,label,hint}` 객체 배열이며, 저장 형식과 무관하게 API 가 정규화한다.

**스키마를 임의로 변경하지 않는다.**
`packages/schema/*.json` 은 팀 계약서다. 필드 추가·enum 변경이 필요해 보이면 코드를 고치지 말고 사용자에게 알린다. 변경은 전원 리뷰 후 PR.

**`lifecycle` 과 `household` 는 다른 축이다. 값을 공유하지 않는다.**
- `lifecycle` ∈ 전입 / 청년 / 결혼·신혼 / 출산·육아 / 노후  ← 5개 고정
- `household` ∈ 1인가구 / 신혼부부 / 유자녀가구 / 한부모 / 다자녀 / 다문화
- "1인가구" 를 lifecycle 에 넣는 것이 가장 흔한 실수다

## 정책 데이터

`data/policies/{policy_id}.json` — 1건 1파일, 파일명 = policy_id.

- 작성·수정 후 반드시 `python scripts/validate_policies.py` 실행
- **이 경로는 main 직접 푸시 금지. PR 로만 올린다** (2인 교차 검수 증빙이 PR 히스토리에 남아야 함)
- `review.parsed_by` 와 `reviewed_by` 가 같으면 CI 가 실패한다. 사람이 원문 대조해야 하므로 검수 기록을 대신 채우지 않는다
- 원문에 없는 값을 추론해서 채우지 않는다. 미표기 항목은 `["공고 확인 필요"]` 로 두고 `contact` 에 담당부서를 기입

### 파싱 규칙 (8/9 전제 검증에서 확정)

**소득 조건은 "기준중위소득 N%" 로 명시된 경우에만 `conditions.income_percentile` 에 넣는다.**
건강보험료 본인부담금·연소득 절대액 등 다른 지표는 `manual_conditions` 로 간다.
- hs-2026-0263: "중위소득 180% 이하" → `conditions.income_percentile`
- hs-2026-2673: "건강보험료 본인부담금 월 129,141원 이하" → `manual_conditions`
- 이유: 절대액 기준은 프로필 입력값으로 판정할 수 없다. `conditions` 에 넣으면 판정 불가능한 값이 판정 경로에 들어간다

**홈페이지 요약과 공고문 원문이 다르면 원문을 따른다.**
담당부서명이 대표적이다 (요약 "청년청소년과" vs 원문 "청년청소년정책과 청년지원팀").

**크롤링 대상은 본청 고시공고가 아니다.**
청년 정책은 구청 게시판(`hscity.go.kr/{구청}/user/bbs/...q_bbsCode=1048`)과
청년지원센터 HEY(`hey.hscity.go.kr`)에 분산돼 있다. 본청만 긁으면 청년 정책이 통째로 빠진다.

전제 검증 결과와 부수 발견 전체는 `docs/assumptions.md` 참조.

## 시크릿

- API 키를 코드에 쓰지 않는다. `os.getenv("ANTHROPIC_API_KEY")` 로만 접근
- `.env` 를 커밋하거나 `git add` 하지 않는다
- 환경변수 항목을 추가하면 `.env.example` 에도 반영한다 (값은 비운 채로)

## 외부 표현

발표자료·README·UI 문구 어디에도 아래 표현을 쓰지 않는다.
- ❌ "LLM 기반 맞춤 분석", "AI 가 추천", "RAG 챗봇"
- ✅ "판정은 규칙 엔진이, AI 는 문서 파싱과 결과 설명을"

챗봇·음성 입력은 대회 산출물이 아니라 정식 런칭 로드맵 항목이다. 구현 제안을 하지 않는다.

**`eligible` 은 "신청 자격 충족" 이지 "수급 확정" 이 아니다.**
자격 충족자 중 배점 순으로 선정하는 경쟁 사업이 있다 (hs-2026-2673 은 건강보험료 낮은 순 선정).
UI·발표자료에서 "확정" 이라는 단어를 쓰지 않고 디스클레이머를 함께 표시한다.
- ✅ "신청 자격을 충족합니다. 최종 선정은 심사 결과에 따릅니다."

## 작업 방식

- 담당 영역 밖의 파일은 수정하지 않는다 (A=scripts/ · B=apps/api/ · C=apps/web/)
- 8/16 이후는 기능 동결 — 신규 코드 추가 금지, 수정만
- 범위를 넘는 리팩터링·의존성 추가 전에 먼저 묻는다. 12일 일정이라 되돌릴 여유가 없다

## 스택

Next.js + Tailwind / FastAPI / Claude API / Vercel + Render

**정책 데이터는 DB 가 아니라 `data/policies/*.json` 파일 스토어에서 서빙한다**
(`apps/api/app/store.py` — 검수 게이트 통과분만 로드). `/health` 의 `db: not_configured` 가 정상 상태다.

PostgreSQL(Supabase) 전환은 **베타 로드맵 항목이지 대회 산출물이 아니다.**
발표·Q&A 에서 현재 구성으로 말하지 않는다 — "지금은 파일 스토어, 베타에서 PostgreSQL 전환" 이 정확한 표현이다.

상세는 @README.md 참조.
